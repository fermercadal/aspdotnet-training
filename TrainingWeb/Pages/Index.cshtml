@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">ASP.NET Training website</h1>
</div>

@foreach (var product in Model.Products)
{
    <h2>@product.Title</h2>
}